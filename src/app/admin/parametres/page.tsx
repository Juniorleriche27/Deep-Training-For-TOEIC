export const dynamic = "force-dynamic";

export default function AdminParametresPage() {
  return (
    <section className="card-surface p-6">
      <h1 className="text-3xl font-extrabold">Parametres admin</h1>
      <p className="muted mt-2 text-sm">Roles, permissions, notifications et configuration systeme.</p>
    </section>
  );
}
